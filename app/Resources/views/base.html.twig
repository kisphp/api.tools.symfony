<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}{{ page_title | default('Kisphp Api Tool') }}{% endblock %}</title>
        {% block stylesheets %}
        <link rel="icon" type="image/x-icon" href="/favicon.ico" />
        <link rel="stylesheet" href="/css/external.css">
        <link rel="stylesheet" href="/css/bundle.css">
        {% endblock %}
    </head>
    <body>

        <nav class="navbar navbar-default">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navigation" aria-expanded="false">
                        <span class="sr-only">{{ 'Navigation' | trans }}</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{ path('api_homepage') }}">{{ 'API' | trans }}</a>
                </div>

                <div class="collapse navbar-collapse" id="main-navigation">

                    {% set route = app.request.attributes.get('_route') %}
                    <ul class="nav navbar-nav">
                        <li{% if route == 'api_json_convert' %} class="active"{% endif %}>
                            <a href="{{ path('api_json_convert') }}">{{ 'Json' | trans }}</a>
                        </li>
                        <li{% if route == 'api_base64_convert' %} class="active"{% endif %}>
                            <a href="{{ path('api_base64_convert') }}">{{ 'Base64' | trans }}</a>
                        </li>
                        <li{% if route == 'api_serialized_convert' %} class="active"{% endif %}>
                            <a href="{{ path('api_serialized_convert') }}">{{ 'Serialized' | trans }}</a>
                        </li>
                        <li{% if route == 'api_responsive' %} class="active"{% endif %}>
                            <a href="{{ path('api_responsive') }}">{{ 'Responsive' }}</a>
                        </li>
                    </ul>

                </div>
            </div>
        </nav>

        {% block container %}
        <div class="container">
            {% block body %}{% endblock %}
        </div>
        {% endblock %}

        {% block javascripts %}
            <script src="/js/external.js"></script>
            <script src="/js/bundle.js"></script>
        {% endblock %}
    </body>
</html>
