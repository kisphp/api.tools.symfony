{% extends '::base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" class="btn btn-primary" value="{{ 'Save' | trans }}" />
    {{ form_end(form) }}

    <textarea name="" id="" cols="30" rows="10">{{ result | raw }}</textarea>

{% endblock %}
